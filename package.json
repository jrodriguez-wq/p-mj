{
  "name": "newellrms",
  "version": "1.0.0",
  "description": "NeWell RMS — Rental Management System",
  "private": true,
  "scripts": {
    "install": "npm install",
    "build": "npm ci --include=dev && npm run build",
    "start": "npm start"
  }
}
