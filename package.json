{
  "name": "newellrms",
  "version": "1.0.0",
  "description": "NeWell RMS — Rental Management System",
  "private": true,
  "scripts": {
    "install": "cd back && npm install",
    "build": "cd back && npm ci --include=dev && npm run build",
    "start": "cd back && npm start"
  }
}
